name: Auto PR for Agent Branches

on:
  push:
    branches:
      - "claude/**"
      - "codex/**"
      - "gemini/**"
      - "copilot/**"
      - "perplexity/**"
      - "grok/**"

jobs:
  auto-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get changed files and classify
        id: classify
        run: |
          if ! git merge-base origin/main HEAD >/dev/null 2>&1; then
            echo "No merge base with origin/main; skipping auto-PR for history-rewrite branch."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            {
              echo "### Auto PR skipped"
              echo
              echo "Branch has no merge base with \`origin/main\`; treating this as a history-rewrite branch."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          CHANGED=$(git diff --name-only origin/main...HEAD)
          if [ -z "$CHANGED" ]; then
            echo "No changes vs main"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"

          RESULT=$(echo "$CHANGED" | python3 .github/scripts/classify_paths.py)
          TIER=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['tier'])")
          echo "tier=$TIER" >> "$GITHUB_OUTPUT"
          echo "result=$RESULT" >> "$GITHUB_OUTPUT"

          echo "### Changed files" >> "$GITHUB_STEP_SUMMARY"
          echo "$CHANGED" >> "$GITHUB_STEP_SUMMARY"
          echo "### Risk tier: $TIER" >> "$GITHUB_STEP_SUMMARY"

      - name: Check for existing PR
        if: steps.classify.outputs.skip != 'true'
        id: check-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          EXISTING=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number // empty' 2>/dev/null || true)
          if [ -n "$EXISTING" ]; then
            echo "pr_exists=true" >> "$GITHUB_OUTPUT"
            echo "pr_number=$EXISTING" >> "$GITHUB_OUTPUT"
          else
            echo "pr_exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Ensure labels exist
        if: steps.classify.outputs.skip != 'true' && steps.check-pr.outputs.pr_exists != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh label create "auto-merge" --color "0E8A16" --description "Low-risk, eligible for auto-merge" --force 2>/dev/null || true
          gh label create "review-required" --color "D93F0B" --description "High-risk, requires Logan's review" --force 2>/dev/null || true
          gh label create "agent-review-pending" --color "BFD4F2" --description "Waiting for agent review grace period before auto-merge" --force 2>/dev/null || true

      - name: Create PR
        if: steps.classify.outputs.skip != 'true' && steps.check-pr.outputs.pr_exists != 'true'
        id: create-pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RISK_TIER: ${{ steps.classify.outputs.tier }}
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          TITLE=$(echo "$BRANCH" | sed 's|^[^/]*/||' | sed 's|-[a-zA-Z0-9]*$||' | tr '-' ' ')
          AGENT=$(echo "$BRANCH" | sed 's|/.*||')
          TIER="$RISK_TIER"
          DIFFSTAT=$(git diff --stat origin/main...HEAD)

          if [ "$TIER" = "low" ]; then
            LABEL="agent-review-pending"
          else
            LABEL="review-required"
          fi

          printf -v BODY '%s\n\n%s\n%s\n%s\n%s\n\n%s\n%s\n%s\n%s\n\n%s\n%s' \
            '## Auto-generated PR' \
            '**Agent:**' "\`$AGENT\`" \
            '**Risk tier:**' "\`$TIER\`" \
            '**Label:**' "\`$LABEL\`" \
            '### Diff stat' \
            '```' "$DIFFSTAT" '```' \
            '---' '*Created by auto-pr workflow*'

          gh pr create \
            --title "$TITLE" \
            --body "$BODY" \
            --base main \
            --head "$BRANCH" \
            --label "$LABEL" 2>&1 || true

      - name: Log grace period start
        if: steps.classify.outputs.skip != 'true' && steps.classify.outputs.tier == 'low' && steps.check-pr.outputs.pr_exists != 'true'
        run: |
          echo "Low-risk PR created with agent-review-pending label." >> "$GITHUB_STEP_SUMMARY"
          echo "Auto-merge is gated: label to \`auto-merge\` to enable auto-merge." >> "$GITHUB_STEP_SUMMARY"
